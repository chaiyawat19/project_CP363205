<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1><%= title %></h1>
    <a href="/admin/listitemuser">กลับไปยังรายการอุปกรณ์ปกติ</a>

    <table border="1">
        <thead>
            <tr>
                <th>ชื่ออุปกรณ์</th>
                <th>ประเภท</th>
                <th>รายละเอียด</th>
                <th>สถานะ</th>
                <th>รูปภาพ</th>
                <th>สถานที่เก็บ</th>
                <th>วันเวลาที่ลบ</th>
                <th>จัดการ</th>
            </tr>
        </thead>
        <tbody>
            <% if (deletedEquipmentList.length === 0) { %>
                <tr>
                    <td colspan="8">ไม่มีอุปกรณ์ที่ถูกลบ</td>
                </tr>
            <% } %>
            <% deletedEquipmentList.forEach(equipment => { %>
                <tr>
                    <td><%= equipment.name %></td>
                    <td><%= equipment.category_id ? equipment.category_id.name : '-' %></td>
                    <td><%= equipment.description %></td>
                    <td> 
                        <% if (equipment.status === "available") { %>
                            <span class="badge bg-success">พร้อมใช้งาน</span>
                        <% } else if (equipment.status === "unavailable") { %>
                            <span class="badge bg-danger">ไม่พร้อมใช้งาน</span>
                        <% } else if (equipment.status === "repair") { %>
                            <span class="badge bg-warning text-dark">อยู่ระหว่างซ่อม</span>
                        <% } else { %>
                            <span class="badge bg-secondary">ไม่ทราบสถานะ</span>
                        <% } %>
                    </td>
                    <td>
                        <% if (equipment.image) { %>
                            <img src="/uploads/<%= equipment.image %>" alt="" width="100" height="100">
                        <% } else { %>
                            -
                        <% } %>
                    </td>
                    <td><%= equipment.location %></td>
                    <td><%= equipment.deleted_at.toLocaleDateString('th-TH') %></td>
                    <td>
                        <!-- ตัวอย่างปุ่มกู้คืน -->
                        <form action="/admin/restoreEquipment/<%= equipment._id %>" method="post" style="display:inline;" onsubmit="return confirm('คุณต้องการกู้คืน <%= equipment.name %>?')">
                            <button type="submit">กู้คืน</button>
                        </form>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</body>
</html>
